# 02-migration

Цель: показать, что миграции применяются отдельным migration runner через HAProxy writer endpoint.

Ожидаемые артефакты:

- `artifacts/migration_version.txt`
- вывод применения миграций
- подтверждение, что writer endpoint ведет на primary
